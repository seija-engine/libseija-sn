<Panel>
    <Image sprite="default.white" color="#eee"/>
    <ListBox width="640" height="480" itemsSource="{Binding Data dataList2}">
        <ListBox.itemTemplate>
            <DataTemplate>
              <Panel isCanvas="true" height="30" >
                <Text color="#ffffff" text="{Binding Data Name}"/>
              </Panel>
            </DataTemplate>
          </ListBox.itemTemplate>
    </ListBox>
    <ButtonBase command="{Binding Data update2Command}" ver="Start" >"测试"</ButtonBase>
</Panel>